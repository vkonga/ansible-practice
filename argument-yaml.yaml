- name: passing argument to command line
  hosts: local
  connection: local
  tasks:
  - name: calling arguments from command line
    ansible.builtin.debug:
      msg: "{{course}}, compelted {{duration}}"